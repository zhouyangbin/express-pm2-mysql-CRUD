{
    "name": "recipes",
    "version": "1.0.0",
    "description": "pm2",
    "author": "zyb",
    "private": true,
    "scripts": {
        "generate": "nuxt generate",
        "start": "set NODE_ENV=dev && node app.js",
        "nodemon": "set NODE_ENV=dev && nodemon app.js",
        "proapp": "set NODE_ENV=pro && node app.js",
        "pm2": "set NODE_ENV=prod && pm2 start processes.json",
        "test": "node app.js & node socket.js"
    },
    "dependencies": {
        "axios": "^0.19.0",
        "concurrently": "^5.2.0",
        "cookie-parser": "^1.4.4",
        "express": "^4.17.1",
        "mysql": "^2.17.1",
        "mysql2": "^2.1.0",
        "nodemon": "^2.0.1",
        "sequelize": "^6.3.4",
        "socket.io": "^2.5.0"
    },
    "devDependencies": {
        "cross-env": "^5.0.1"
    }
}
