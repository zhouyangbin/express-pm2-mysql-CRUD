{
  "name": "recipes",
  "version": "1.0.0",
  "description": "pm2",
  "author": "zyb",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "generate": "nuxt generate",
    "start": "set NODE_ENV=dev && node app.js",
    "nodemon": "set NODE_ENV=dev && nodemon app.js",
    "proapp": "set NODE_ENV=pro && node app.js",
    "pm2": "set NODE_ENV=prod && pm2 start processes.json"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "cookie-parser": "^1.4.4",
    "express": "^4.17.1",
    "nodemon": "^2.0.1"
  },
  "devDependencies": {
    "cross-env": "^5.0.1"
  }
}
