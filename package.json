{
  "name": "recipes",
  "version": "2.0.0",
  "description": "My outstanding Nuxt.js project",
  "author": "allenChinese",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "devapp": "set NODE_ENV=dev && node app.js",
    "proapp": "set NODE_ENV=pro && node app.js",
    "pm2": "set NODE_ENV=prod && pm2 start processes.json"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "cross-env": "^5.0.1"
  }
}
